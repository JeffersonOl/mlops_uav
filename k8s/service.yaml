apiVersion: v1
kind: Service
metadata:
  name: uav-inference-service
  labels:
    app: uav-inference
spec:
  type: NodePort
  selector:
    app: uav-inference
  ports:
  - name: http
    protocol: TCP
    port: 5000
    targetPort: 5000
    nodePort: 30500
